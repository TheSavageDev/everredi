steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/everredi-api-staging:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/everredi-api-staging:latest'
      - '-f'
      - 'Dockerfile'
      - '.'

  # Push to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/everredi-api-staging:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/everredi-api-staging:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'everredi-api-staging'
      - '--image'
      - 'gcr.io/$PROJECT_ID/everredi-api-staging:$SHORT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'NODE_ENV=production,FIREBASE_PROJECT_ID=$PROJECT_ID,FIREBASE_DATABASE_ID=${_DATABASE_ID},CORS_ORIGIN=${_CORS_ORIGIN}'
      - '--cpu-boost'
      - '--set-secrets'
      - 'FIREBASE_PRIVATE_KEY=firebase-private-key-${_ENV}:latest,FIREBASE_CLIENT_EMAIL=firebase-client-email-${_ENV}:latest,STRIPE_SECRET_KEY=stripe-secret-key-${_ENV}:latest,STRIPE_WEBHOOK_SECRET=stripe-webhook-secret-${_ENV}:latest,GEMINI_API_KEY=gemini-api-key-${_ENV}:latest,REVENUECAT_SECRET_API_KEY=revenuecat-secret-api-key-${_ENV}:latest'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--timeout'
      - '300'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _ENV: 'staging'
  _DATABASE_ID: 'staging'
  _CORS_ORIGIN: 'https://staging.everredi.com'

timeout: '1200s'
