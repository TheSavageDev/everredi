# Archived migrations

These files (002–018) are **not** used for new installs. They are kept for reference only.

For a fresh database, run from the parent directory:

1. `000_consolidated_schema.sql` (schema + RLS)
2. `001_seed_supply_catalog.sql` (optional seed)
